import React from 'react';
import './Toolbar.css';

const toolbar = props => (
  <header className="toolbar">
    <nav className="toolbar-nav">
      <div></div>
      <div className="logo"><a href="/">MyApp</a></div>

    </nav>
  </header>

);

export default toolbar;
